<!DOCTYPE html>
<html lang="ko">
  <head>
    {{> ../partials/meta}}
    {{> ../partials/link}}
    {{> ../partials/script}}
    <title>Sample User Info</title>
  </head>
  <body class="bg-gray-50 py-10">
    <div class="mx-auto max-w-2xl rounded-lg bg-white p-8 shadow">
      <h1 class="mb-6 text-2xl font-bold text-gray-800">사용자 정보</h1>
      <div class="mb-8">
        <div class="grid grid-cols-2 gap-4">
          <div class="font-semibold text-gray-600">ID</div>
          <div>{{ user.id }}</div>
          <div class="font-semibold text-gray-600">이름</div>
          <div>{{ user.name }}</div>
          <div class="font-semibold text-gray-600">이메일</div>
          <div>{{ user.email }}</div>
          <div class="font-semibold text-gray-600">활성화</div>
          <div>
            {{#if user.isActive}}
            <span
              class="inline-block rounded bg-green-100 px-2 py-1 text-xs text-green-700"
              >활성</span
            >
            {{else}}
            <span
              class="inline-block rounded bg-red-100 px-2 py-1 text-xs text-red-700"
              >비활성</span
            >
            {{/if}}
          </div>
          <div class="font-semibold text-gray-600">가입일</div>
          <div>{{ user.createdAt }}</div>
        </div>
      </div>

      <h2 class="mb-4 text-xl font-bold text-gray-700">주문 내역</h2>
      <div class="space-y-8">
        {{#each orders}}
        <div class="rounded-lg border bg-gray-50 p-4">
          <div class="mb-2 flex flex-wrap items-center justify-between">
            <div>
              <span class="font-semibold text-gray-700">주문번호:</span>
              <span class="ml-1 text-gray-900">{{ orderId }}</span>
            </div>
            <div>
              <span class="font-semibold text-gray-700">주문일:</span>
              <span class="ml-1 text-gray-900">{{ date }}</span>
            </div>
            <div>
              <span class="font-semibold text-gray-700">상태:</span>
              <span class="ml-1 text-blue-700">{{ status }}</span>
            </div>
            <div>
              <span class="font-semibold text-gray-700">총액:</span>
              <span class="ml-1 text-gray-900">{{ totalAmount }}원</span>
            </div>
          </div>
          <table class="mt-3 w-full border-t text-sm">
            <thead>
              <tr class="bg-gray-100">
                <th class="px-2 py-2 text-left font-semibold text-gray-600">
                  상품명
                </th>
                <th class="px-2 py-2 text-right font-semibold text-gray-600">
                  수량
                </th>
                <th class="px-2 py-2 text-right font-semibold text-gray-600">
                  가격
                </th>
              </tr>
            </thead>
            <tbody>
              {{#each items}}
              <tr>
                <td class="px-2 py-2">{{ productName }}</td>
                <td class="px-2 py-2 text-right">{{ quantity }}</td>
                <td class="px-2 py-2 text-right">{{ price }}원</td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
        {{/each}}
      </div>
    </div>
  </body>
</html>
